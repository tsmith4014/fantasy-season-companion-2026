{
  "name": "fantasy-season-companion-2026",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A local-first 2026 fantasy football companion for waivers, lineups, league analysis, and trades.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "clean": "node scripts/clean.mjs",
    "lint": "node scripts/lint.mjs",
    "privacy:check": "node scripts/check-private-data.mjs",
    "snapshot:validate": "node scripts/validate-snapshot.mjs",
    "test": "node --test",
    "check": "npm run lint && npm run snapshot:validate && npm test && npm run build && npm run privacy:check",
    "serve": "node scripts/serve.mjs dist 4174"
  },
  "engines": {
    "node": ">=20.11"
  }
}
